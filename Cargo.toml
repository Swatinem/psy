[package]
name = "psy"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
criterion = "0.3"
rand = { version = "0.8.3", features = ["small_rng"] }

[[bench]]
name = "lookup"
harness = false

[profile.bench]
debug = 2

[profile.release]
debug = 2
